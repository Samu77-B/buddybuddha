/**
 * Copy this file to checkout-config.js and set your checkout API URL.
 * checkout-config.js is gitignored — do not commit secrets (only the public API URL goes here).
 */
window.BB_CHECKOUT = {
  apiBase: "http://localhost:4242",
};
